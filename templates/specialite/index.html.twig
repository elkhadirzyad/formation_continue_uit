{% extends "base.html.twig" %}


{% block body %}

<style>
        .lienAnnonceCategorie {
            font-size: 16px;
            font-weight: bold;
            color: #25427c;
        }

        .lienAnnonceCategorie a:hover,
        .lienAnnonceCategorie a:visited,
        .lienAnnonceCategorie a:active {
            text-decoration: none;
        }
    </style>
<div class="area2 col-md-6 ">
    <span id="scroll"></span>
    <h2 class="title green" id="titreEvents"><span class="leTitre">DOMAINES DE SPECIALITÉ <b>>></b> {{spt.titre}}</span>
        <a href="{{path('home')}}" style="font-size: 25px; margin-top: -6px; margin-left: 40px;"><span class="fa fa-home"></span></a></h2>
    <center><img id="loaderAnnonce" src="/img/Front/loader2.gif" style="width: 100px; display: none;"></center>
    <div class="row-fix" id="detailAnnonce">


        




        <div style="margin-left: 20px; margin-top: 20px;">
            <!-- liste des masters si elles existent-->
        <p class="lienAnnonceCategorie" categorie="#" annonce="#"> <span class="fa fa-graduation-cap"></span> Liste des masters</p>
        

            
            <div id="table_master" >
            <tr>
                    <td>{{mus.titre}} </td>
                    <td>Pr.{{mus.responsable.nom}}</td>
                    <td>{{mus.code}}</td>
                    <td>{{mus.etablissement}}</td>
                    <td>{{mus.dateouverture}}</td>
                    <td>descriptif | candidature</td>
            </tr>

            </div>


            </table>

            <br> 

            <!-- liste des licences si elles existent-->

            <p class="lienAnnonceCategorie" categorie="#" annonce="#"> <span class="fa fa-graduation-cap"></span> Liste des licences</p>
            <table cellspacing='0' width='80%' id='example' class='table table-striped table-hover table-responsive'>	<thead>
    
                 
                   <tr>
                    <th>titre </th>
                    <th>Responsable</th>
                    <th>Code</th>
                    <th>Etablissement</th>
                    <th>Année d'ouverture</th>
                    <th>Documents</th> </tr>
                    </thead>
                   
               
    
                    <div id="table_licence" >
    
                <tr>
                        <td>{{lus.titre}}</td>
                        <td>Pr.{{lus.responsable.nom}}</td>
                        <td>{{lus.code}}</td>
                        <td>{{lus.etablissement}}</td>
                        <td>{{lus.dateouverture}}</td>
                        <td>descriptif | candidature</td>
                </tr>
                </div>
               
    
    
                {% endfor %}
                </table>

                

        </div>
    </div>
</div>

{% endblock %}