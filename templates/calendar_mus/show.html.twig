{% extends './resp/base.html.twig' %}

{% block title %}CalendarMus{% endblock %}

{% block body %}
    <h1>Cours Master: {{ calendar_mus.title }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ calendar_mus.id }}</td>
            </tr>
            <tr>
                <th>BeginAt</th>
                <td>{{ calendar_mus.beginAt ? calendar_mus.beginAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>EndAt</th>
                <td>{{ calendar_mus.endAt ? calendar_mus.endAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('calendar_mus') }}"  class="btn btn-primary">emploi du temps</a>
     
    <br><br>

    <a href="{{ path('calendar_mus_edit', {'id': calendar_mus.id}) }}" class="btn btn-primary">edit</a>
     
    <br><br>

    {{ include('calendar_mus/_delete_form.html.twig') }}
{% endblock %}
