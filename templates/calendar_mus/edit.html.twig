{% extends './resp/base.html.twig' %}

{% block title %}Edit CalendarMus{% endblock %}

{% block body %}
<div class="col-lg-12">
        <div class="panel panel-primary" style="margin-top:20px;">
            <div class="panel-heading">
              Editer cours 
            </div>
            <!-- /.panel-heading -->
            <div class="panel-body">

    {{ include('calendar_mus/_form.html.twig', {'button_label': 'Update'}) }}
    <br>
    <a href="{{ path('calendar_mus') }}"  class="btn btn-primary">Emploi du temps</a>
     
    <br>
    <br>
    {{ include('calendar_mus/_delete_form.html.twig') }}
    </div>
    </div>
    </div>
{% endblock %}
